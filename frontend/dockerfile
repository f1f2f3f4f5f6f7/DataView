FROM node:20-alpine
WORKDIR /app

# Instala dependencias primero (caching eficiente)
COPY package*.json ./
RUN npm install

# El código fuente se monta como volumen en dev
EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]